/** @type {import('tailwindcss').Config} */

const config = {
  content: ["./**/*.html", "!./node_modules"],
  darkMode: "selector",
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;